# Decision Coach Environment Variables

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI API Key (Optional - Not required for Decision Intelligence Agent)
# The app now uses a built-in Structured SLM that works offline
# You only need this key if you want to switch to OpenAI in the future
# VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# App Configuration
VITE_APP_NAME=Decision Coach
VITE_APP_URL=http://localhost:5173

# Note: The Decision Intelligence Agent (SLM) works completely offline
# and does not require any API keys. It provides structured decision analysis
# based on rule-based algorithms designed for decision intelligence.
